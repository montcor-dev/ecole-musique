<p style="color: green"><%= notice %></p>

<%= render @interaction %>

<div>
  <%= link_to "Edit this interaction", edit_interaction_path(@interaction) %>
  <br>
  <%= link_to "Back to interactions", interactions_path %>

  <%= button_to "Destroy this interaction", @interaction, method: :delete %>
</div>

<br>
<div>
  <%= link_to "Retour à la page de l'élève", student_path(@interaction.student_id) %>
</div>
